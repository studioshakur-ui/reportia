import React from "react";
import { t } from "../../lib/i18n";

export default function ManagerHome(){
  return (
    <div className="p-4">
      <h2 className="text-2xl font-bold mb-3">{t("appName")} — {t("role_manager")}</h2>
      <div className="opacity-80">
        {/* Ici ton planner/organigramma/assegnazioni */}
        Gestione pianificazione, assegnazioni squadre, esportazione PDF…
      </div>
    </div>
  );
}
